# --- Secrets ---
# NEVER commit your environment files. They contain passwords and API keys.
.env
*.env

# --- Python ---
# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environment directories
# Used by venv, virtualenv, pyenv, poetry, etc.
venv/
env/
.venv/
.env/

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg

# --- Data Files ---
# Ignore all large data files. The repository should only contain the code
# to process the data, not the data itself.
# It's good practice to add a README.md inside the /data folder to explain
# where to get the data from, and keep that README file.
data/*
!data/README.md

# --- Trained Models & Checkpoints ---
# Trained models are large binary files and should not be versioned in Git.
# The repository should contain the code to TRAIN the models, not the models themselves.
models/*
!models/README.md
models/checkpoints/

# --- Logs and Outputs ---
# Ignore log files and any output generated by scripts.
logs/
output/
evaluation_summary.json

# --- IDE / Editor specific files ---
# Visual Studio Code
.vscode/

# PyCharm
.idea/

# Jupyter Notebook checkpoints
.ipynb_checkpoints

# --- OS-specific files ---
# macOS
.DS_Store

# Windows thumbnail cache
Thumbs.db
ehthumbs.db

# --- Docker ---
# Ignore data persisted by Docker containers if mapped locally
neo4j_data/